version: '3.3'
services:
  postgres:
    image: "postgres:14.2"
    restart: always
    environment:
      POSTGRES_DB: "ironbank"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "1234"
    ports:
      - "15432:5432"
